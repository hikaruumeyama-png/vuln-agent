# ===========================================
# Gmail設定
# ===========================================

# 【個人Gmail用】OAuthトークン（setup_gmail_oauth.py で自動設定）
# GMAIL_OAUTH_TOKEN=（Base64エンコードされたトークン）

# 【Google Workspace用】ドメイン委任でアクセスするユーザーのメールアドレス
# GMAIL_USER_EMAIL=your-email@example.com

# SIDfmの送信元メールアドレス
SIDFM_SENDER_EMAIL=noreply@sidfm.com

# ===========================================
# SBOM設定（スプレッドシート）
# ===========================================

# スプレッドシートID（URLから取得: https://docs.google.com/spreadsheets/d/【この部分】/edit）
SBOM_SPREADSHEET_ID=your-spreadsheet-id

# シート名
SBOM_SHEET_NAME=SBOM
OWNER_SHEET_NAME=担当者マッピング

# ===========================================
# SBOMデータソース切替
# ===========================================

# sheets / bigquery / auto から選択（デフォルト: sheets）
SBOM_DATA_BACKEND=sheets

# ===========================================
# BigQuery設定（SBOM_DATA_BACKEND=bigquery 時に使用）
# ===========================================

# BigQuery テーブル ID（project.dataset.table 形式）
# BQ_HISTORY_TABLE_ID=your-project.vuln_agent.incident_response_history
# BQ_SBOM_TABLE_ID=your-project.vuln_agent.sbom_packages
# BQ_OWNER_MAPPING_TABLE_ID=your-project.vuln_agent.owner_mapping

# ===========================================
# Google Chat設定
# ===========================================

# 通知先スペースID（spaces/XXXXX形式）
# 以下の優先順位で参照されます:
#   1. DEFAULT_CHAT_SPACE_ID
#   2. CHAT_SPACE_ID
#   3. GOOGLE_CHAT_SPACE_ID
# いずれも未設定の場合は Secret Manager (vuln-agent-chat-space-id) から取得
DEFAULT_CHAT_SPACE_ID=spaces/your-space-id
# CHAT_SPACE_ID=spaces/your-space-id
# GOOGLE_CHAT_SPACE_ID=spaces/your-space-id

# Chat app用サービスアカウント鍵JSON（Agent Engine上で必須）
# ローカルではGOOGLE_APPLICATION_CREDENTIALSで代替可能
# Agent Engine上ではSecret Manager (vuln-agent-chat-sa-key) から自動取得
# CHAT_SA_CREDENTIALS_JSON={"type":"service_account",...}

# ===========================================
# GCP設定（オプション）
# ===========================================

# プロジェクトID（通常は自動検出）
# GCP_PROJECT_ID=your-project-id

# リージョン
# GCP_LOCATION=asia-northeast1
